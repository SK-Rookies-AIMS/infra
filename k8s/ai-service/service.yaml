apiVersion: v1
kind: Service
metadata:
  name: ai-service-api
  namespace: ai-services
  labels:
    app: ai-service-api
spec:
  type: ClusterIP
  selector:
    app: ai-service-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
