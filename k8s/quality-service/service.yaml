apiVersion: v1
kind: Service
metadata:
  name: quality-service
  namespace: aims-project
  labels:
    app: quality-service
spec:
  type: ClusterIP
  selector:
    app: quality-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
